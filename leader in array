#include <iostream> 
using namespace std; 
int main()
{
  int arr[]={14,5,9,7,2,1,3} ;
int n= sizeof(arr)/sizeof(arr[0]); 
int max; 
max=arr[n-1]; 
cout<<max<<endl; 
for(int i=n-2;i>=0;i--){
    if(arr[i]>max){
        max=arr[i]; 
        cout<<max<<endl;
    }
}


    return 0;
}
